<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Statistics - SkyTech</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles/style.css">
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">SkyTech</a>
            </div>
            <ul class="nav navbar-nav navbar-right">
                <li><a href="/logout">Logout</a></li>
            </ul>
        </div>
    </nav>
    <div class="container" style="margin-top: 60px;">
        <div class="text-center">
            <h1>Statistics</h1>
        </div>

        <section class="mt-4">
            <h2>Relevant Parts</h2>
            <ul class="list-group">
                <% parts.forEach(function(part) { %>
                    <li class="list-group-item"><%= part.name %>: <%= part.quantity %></li>
                <% }); %>
            </ul>
        </section>

        <section class="mt-4">
            <h2>Least Available Parts</h2>
            <ul class="list-group">
                <% leastAvailableParts.forEach(function(part) { %>
                    <li class="list-group-item"><%= part.name %>: <%= part.quantity %></li>
                <% }); %>
            </ul>
        </section>

        <div class="text-center mt-5">
            <a href="/dashboard" class="btn btn-info btn-lg" role="button">Back to Dashboard</a>
        </div>
    </div>
    <footer class="text-center" style="margin-top: 20px;">
        <p>Projeto Final PW 23/24</p>
        <p>Diogo Silva, Joel Tapia, Bruno Bento, João Mendonça</p>
    </footer>
</body>
</html>
